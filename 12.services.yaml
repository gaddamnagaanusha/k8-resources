apiversion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:      
  #labels are used as selector to identify the pods which are part of this service
    project: roboshop
    compenent: frontend
    environment: prod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80   #container port which is exposed by the pod