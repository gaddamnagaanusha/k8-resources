apiversion: v1
kind: service
metadata:
  name: nginx-ln
spec:
  type: LoadBalancer    # service type is LoadBalancer
  selector:
    project: roboshop
    compenent: frontend
    environment: prod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

 